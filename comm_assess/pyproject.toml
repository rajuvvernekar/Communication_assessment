[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "comm_assess"
authors = [{name = "Your Organization", email = "admin@example.com"}]
description = "Corporate Communication Assessment Platform for Frappe Framework"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[tool.flit.module]
name = "comm_assess"
